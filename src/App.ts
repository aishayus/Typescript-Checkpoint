import Car from "./car";

// Create an instance of the Car class
const myCar = new Car('Toyota', 'Corolla', 2021);

// Call the start method to see the output
myCar.start(); // This will log: "Car engine started"
